---
description: 
globs: 
alwaysApply: true
---
**6. Code Style & Formatting (Frontend):**

*   Formatter avec Prettier, linter avec ESLint (configs: `.prettierrc`, `eslint.config.js`).
*   **Assurer que TOUT le code (écrit ou généré) respecte ces configurations.**
*   Exécuter `npm run format` / `npm run lint` via Sail (`sail npm run format`) pour vérifier/corriger.
